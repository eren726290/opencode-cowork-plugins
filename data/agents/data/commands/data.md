---
description: Data plugin master hub command listing all available workflows
argument-hint: "[optional goal or command name]"
---

# /data

Data Plugin — Available Commands:

/data               -> Full data plugin power in one prompt
/analyze            -> Answer data questions & full analyses
/explore-data       -> Profile & explore datasets
/write-query        -> Write optimized SQL for your dialect
/create-viz         -> Create visualizations with Python
/build-dashboard    -> Build interactive HTML dashboards
/validate           -> QA an analysis before sharing

Type any command to get started.

If the user provides a goal in `$ARGUMENTS`, route to the most relevant command and ask only missing minimum inputs.
